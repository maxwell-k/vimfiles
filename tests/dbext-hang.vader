After:
  DBResultsClose

" Setup
Execute (Function to hide irrelevant details):
  source ./dbext-mask.vim " defines DBextPostResult

Given (Placeholder):
  [placeholder]

Execute (Syncronous first select):
  let g:dbext_default_job_enable=0
  Select 1;
  buffer Result

Expect (1 in one column with a header):
  Connection: T(SQLITE)  D(XXXXX)   at XX:XX
  1
  ----------
  1

Execute (Syncronous second select):
  Select 2;
  buffer Result

Expect (2 in one column with a header):
  Connection: T(SQLITE)  D(XXXXX)   at XX:XX
  2
  ----------
  2

" Tear down
Execute (Clear function):
  delfunction DBextPostResult
