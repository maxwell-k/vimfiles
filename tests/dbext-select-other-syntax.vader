Execute (Use the :Select syntax):
  call my_dbext#load()
  Select 3;

Then (Check async and view the result):
  AssertEqual 1, g:dbext_default_use_jobs
  sleep 250m
  DBResultsClose " must be after any sleep
  buffer Result
  call vim#dbext_mask()

Expect (3 in one column with a header):
  Connection: T(SQLITE)  D(XXXXX)   at XX:XX
  job started:XX:XX:XX updates every 2000 ms
  3
  ----------
  3
